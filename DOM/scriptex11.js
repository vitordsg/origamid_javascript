// Retorne o url da página atual utilizando o objeto window
const urlPag = window.location.href
console.log(urlPag)


// Seleciona o primeiro elemento da página que
// possua a classe ativo
const h1selection = document.querySelector('.titulo')
console.log(h1selection)


// Retorne a linguagem do navegador
const linguage = window.navigator.language
console.log(linguage)

// Retorne a largura da janela 
const larguraJanela = window.innerHeight
console.log(larguraJanela)
